services:
  rust-exporter:
    build: ./rust-exporter
    container_name: rust-exporter
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3000"
    volumes:
      - /proc:/proc:ro
      - /sys:/sys:ro