services:
  rust-exporter:
    build:
      context: ./rust-exporter
      args:
        GIT_HASH: ${GIT_HASH:-unknown}
    container_name: rust-exporter
    restart: unless-stopped
    network_mode: host
    volumes:
      - /proc:/proc:ro
      - /sys:/sys:ro