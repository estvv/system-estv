services:
  system-estv:
    build:
      context: ./system-estv
      args:
        GIT_HASH: ${GIT_HASH:-unknown}
    container_name: system-estv
    restart: unless-stopped
    network_mode: host
    volumes:
      - /proc:/proc:ro
      - /sys:/sys:ro