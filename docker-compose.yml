services:
  system-estv:
    build:
      context: ./system-estv
    container_name: system-estv
    restart: unless-stopped
    network_mode: host
    volumes:
      - /proc:/proc:ro
      - /sys:/sys:ro